source("renv/activate.R")
# Silence the "project is out of sync" warning printed on every
# Rscript invocation. We install the local nccs.data.efile package
# via renv::install("."), which intentionally diverges from the
# lockfile. The check is noisy and provides no signal here.
options(renv.config.synchronized.check = FALSE)
