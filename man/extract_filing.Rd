% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_filing.R
\name{extract_filing}
\alias{extract_filing}
\title{Extract a single 990 filing into a one-row data.frame}
\usage{
extract_filing(row, dict, version = NULL)
}
\arguments{
\item{row}{A single-row data.frame or named list. Must carry
either \code{s3_key} (anonymous S3 fetch via https) or \code{local_path}
(already-downloaded XML).}

\item{dict}{A dictionary data.frame; see \code{load_dictionary()}.}

\item{version}{XSD version string to resolve XPath claims for.
If \code{NULL} (the default at scale), the version is parsed from the
row's \code{return_version} column (e.g. \code{"2024v5.1"} -> \code{"5.1"}). An
explicit value is still accepted for single-version test slices.}
}
\value{
A one-row data.frame with key columns + one column per
\code{nccs_name} + \verb{_extract_error} (character, \code{NA} on success).
}
\description{
Given an index row (with at minimum \code{s3_key}, \code{ein}, \code{tax_year},
\code{form_type}, \code{filing_receipt_id}) and a layer-2 dictionary,
downloads the XML, applies each dictionary row's XPath claim for
the filing's \verb{(tax_year, version)}, and returns one row with one
column per dictionary \code{nccs_name}.
}
\details{
Designed to run inside \code{furrr::future_map}: on any failure
(network, parse, missing element, type coercion) the function
returns a row of \code{NA}s plus an \verb{_extract_error} column rather than
aborting the whole vintage build.

See ADR 0001 section 9. The same surface is used by the IRS direct
fallback - the only difference is how the XML is obtained.
}
