% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_relational.R
\name{extract_relational}
\alias{extract_relational}
\title{Wholesale relational (scalar) extraction — ADR 0004 step 2.}
\description{
Where \code{extract_filing()} pulls only the curated dictionary's claims, the
relational path pulls \strong{every non-repeating scalar leaf} for a filing's
form, driven by the Layer 1 inventory's \code{is_leaf & !repeating} set (the
\code{repeating} flag added in ADR 0004 step 1). One parse of the DOM feeds every
leaf, so the marginal cost over the dictionary path is small — the parse
dominates (see the parse-cost note in \code{extract_filing}).
}
\details{
The output is the raw, XSD-faithful researcher tier: per-form \emph{header} tables
(\code{f990_header}, \code{f990pf_header}) plus the shared \code{returnheader}, one row per
filing, columns named by the element's path \strong{relative to the form root}
(NOT snake_case — that is the curated views' job). Best-effort / uncontracted
(ADR 0004 section 4 / nccs-contracts 0028).

This file is the in-memory machinery (extract + assemble + type). Publishing
to \verb{relational/\{table\}/\{vintage\}/} with a manifest + dictionary is ADR 0004
step 3.
}
