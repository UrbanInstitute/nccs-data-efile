% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xsd_inventory.R
\name{build_all_xsd_inventories}
\alias{build_all_xsd_inventories}
\title{Build the Layer 1 inventory across all configured (tax_year, version)
cells and concatenate into one frame.}
\usage{
build_all_xsd_inventories(config = load_config(), roots = NULL)
}
\arguments{
\item{config}{Loaded config list.}

\item{roots}{Optional form-root list (defaults to 990+990PF+header).}
}
\value{
A data.frame of all cells' rows with an added
\code{xsd_version_used} column.
}
\description{
Loops \code{config$xsd$versions}, honoring \code{version_aliases} (a (year,
version) whose XSDs are inherited records the actually-used XSD
version in \code{xsd_version_used}). A cell that fails to build (missing
XSD cache, parse error) is warned and skipped, not fatal.
}
