% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_relational.R
\name{build_relational_plan}
\alias{build_relational_plan}
\title{Build the relational extraction plan from a Layer 1 inventory.}
\usage{
build_relational_plan(inventory, tables = default_relational_tables())
}
\arguments{
\item{inventory}{A Layer 1 inventory data.frame (must carry \code{xpath},
\code{xsd_type}, \code{is_leaf}, \code{repeating}, and ideally \code{annotation}).}

\item{tables}{Table/root spec; defaults to 990 + 990PF + returnheader.}
}
\value{
A named list (by table) of \code{list(table, root, forms, leaves)} where
\code{leaves} is a data.frame of \verb{xpath, col_name, xsd_type, target_type, annotation} — the per-table dictionary, deduplicated on \code{xpath}.
}
\description{
For each table, the column set is the union of that form's non-repeating
scalar leaves over every (tax_year, version) cell in the inventory — a filing
populates the columns its version defines, the rest stay null (ADR 0004
section 3).
}
