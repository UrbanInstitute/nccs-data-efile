% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_relational.R
\name{extract_filings_relational}
\alias{extract_filings_relational}
\title{Extract a batch of filings into assembled, typed relational tables.}
\usage{
extract_filings_relational(index, plan, max_bytes = 5e+07, progress = FALSE)
}
\arguments{
\item{index}{A data.frame of index rows (see \code{fetch_gt_indices()}).}

\item{plan}{A plan from \code{build_relational_plan()}.}

\item{max_bytes}{Per-filing size cap.}

\item{progress}{Show a furrr progress bar.}
}
\value{
A named list (by table) of typed data.frames.
}
\description{
Runs \code{extract_filing_relational()} in parallel (\code{furrr}), unions each table's
rows (filling version-absent columns with NA), then types every value column
per the plan with the raw-string fallback. Returns the header tables in
memory; publishing is ADR 0004 step 3.
}
