% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emit_manifest.R
\name{emit_manifest}
\alias{emit_manifest}
\title{Emit a per-vintage manifest.json}
\usage{
emit_manifest(
  vintage = NULL,
  phase,
  scope,
  parquet_paths,
  inputs,
  xsd_verification,
  value_distribution,
  producer_git_sha = NULL,
  out_dir
)
}
\arguments{
\item{vintage}{Vintage string, e.g. \code{"v2026.06"}. If \code{NULL},
derived from today as \code{vYYYY.MM}.}

\item{phase}{Phase label, e.g. \code{"phase0"}.}

\item{scope}{List with \code{forms} and \code{tax_years}.}

\item{parquet_paths}{Character vector of paths to the published
parquet files. Row counts are read via \code{arrow::read_parquet}
if available; otherwise reported as \code{NA}.}

\item{inputs}{Named list to populate the \code{inputs} block
(NODC SHA, GT lake snapshot timestamp, XSD cache prefix, etc.).}

\item{xsd_verification}{Result list from \code{run_phase0_verification()}.}

\item{value_distribution}{Result list from \code{verify_value_distribution()}.}

\item{producer_git_sha}{Optional commit SHA; auto-detected via
\verb{git rev-parse HEAD} when \code{NULL}.}

\item{out_dir}{Directory to write the manifest into.}
}
\value{
Invisibly, the path to the written manifest.
}
\description{
Writes \verb{_manifest.json} to \code{out_dir} per the shape in ADR 0001
section 10. Computes sha256 + byte size + row count for each
parquet file in \code{parquet_paths}.
}
