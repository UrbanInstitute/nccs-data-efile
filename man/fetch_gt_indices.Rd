% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_gt_indices.R
\name{fetch_gt_indices}
\alias{fetch_gt_indices}
\title{Fetch and filter GT data lake indices}
\usage{
fetch_gt_indices(
  config = load_config(),
  local_cache_dir = NULL,
  skip_sync = FALSE
)
}
\arguments{
\item{config}{Loaded config list.}

\item{local_cache_dir}{Where to cache yearly CSVs. Defaults to
\verb{~/.cache/nccs-data-efile/gt-indices/}.}

\item{skip_sync}{If \code{TRUE}, do not fetch from S3; just re-parse
whatever is on disk.}
}
\value{
A data.frame with one row per filing in scope.
}
\description{
Reads the per-tax-year CSVs under
\verb{s3://gt990datalake-rawdata/Indices/990xmls/yearly/} (anonymous
read), keeps the most recent snapshot per year, parses, filters by
scope, and returns a single data.frame of filings to extract.
}
\details{
The full \verb{Indices/990xmls/} prefix is ~358 GB across many
snapshots; this function pulls only the latest snapshot per
in-scope tax_year, which keeps a single-year fetch under a minute.

Per ADR 0001 section 9.

GT yearly index columns (post-2025 snapshots):
\verb{BuildTs, DAF, DateSigned, DocStatus, EIN, FileSha256, FileSizeBytes, FormType, GrossReceipts, ..., ObjectId, ..., ReturnVersion, ..., TaxPeriod, TaxYear, ..., URL, ZipFile}

Normalized output columns:
\verb{filing_receipt_id, ein, tax_period, tax_year, form_type, return_version, object_id, url, s3_key}

\code{s3_key} is \verb{s3://} form of \code{url} for parity with the
\code{fetch_irs_direct()} shape; downstream \code{extract_filing()} prefers
the local_path or s3_key fields.
}
