% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_relational.R
\name{run_relational}
\alias{run_relational}
\title{Run a wholesale relational (scalar) extraction build — ADR 0004 step 3.}
\usage{
run_relational(
  config = load_config(),
  vintage = NULL,
  out_dir = NULL,
  index = NULL,
  inventory = NULL,
  plan = NULL
)
}
\arguments{
\item{config}{Loaded config list.}

\item{vintage}{Vintage string (\code{v2026.06}); auto-derived if \code{NULL}.}

\item{out_dir}{Output directory (default \code{{output$local_dir}/relational/{vintage}}).}

\item{index}{Optional pre-built index (skips the fetch; for testing).}

\item{inventory}{Optional Layer 1 inventory data.frame (skips the rebuild).}

\item{plan}{Optional pre-built relational plan (skips inventory + plan build).}
}
\value{
Invisibly, a summary list (vintage, out_dir, per-table row counts).
}
\description{
Drives the raw researcher tier end-to-end:
\enumerate{
\item Build the relational plan from the Layer 1 inventory (\code{is_leaf & !repeating} scalar leaves per form root).
\item Fetch the upstream index.
\item Extract every in-scope filing in parallel, one parse per filing feeding
all of \code{returnheader} + the form body, via the ZIP-bulk staging path
(the same engine \code{run_phase0()} uses) or the per-filing \code{objects} path.
\item Write each table partitioned by \code{tax_year}, with a per-table dictionary,
a best-effort/uncontracted marker, and a manifest.
}
}
\details{
S3 publication is NOT here — \code{inst/scripts/run_relational.R} syncs each table
dir to \verb{relational/\{table\}/\{vintage\}/} after a clean build, so a failed build
never half-publishes. The tier is best-effort / uncontracted (ADR 0004
section 4 / nccs-contracts 0028): provenance is shipped, stability is not
promised.
}
