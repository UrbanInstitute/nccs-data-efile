% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spot_check_vintage.R
\name{spot_check_vintage}
\alias{spot_check_vintage}
\title{Held-out spot-check of a published Phase 0 vintage}
\usage{
spot_check_vintage(
  vintage_dir,
  n_per_form = 10,
  index = NULL,
  config = load_config(),
  dict = load_dictionary(),
  seed = 42,
  report_path = NULL
)
}
\arguments{
\item{vintage_dir}{Local path to the vintage directory, OR an
\verb{s3://...} URI. Remote vintages are pulled to a temp dir.}

\item{n_per_form}{How many filings per \code{form_type} to sample.
ADR 0002 specifies 10.}

\item{index}{Optional pre-loaded GT index data.frame. When \code{NULL},
calls \code{fetch_gt_indices()} to recover URL + return_version.}

\item{config}{Loaded config list.}

\item{dict}{Layer-2 dictionary; used to look up the documented
XPath per \verb{(tax_year, version)}.}

\item{seed}{Random seed for reproducibility.}

\item{report_path}{Optional path to write a JSON report.}
}
\value{
A list with \code{n}, \code{agree}, \code{disagree}, \code{per_form}, and a
data.frame \code{details} (one row per sampled filing).
}
\description{
Samples N random filings per form_type from a vintage's parquet
files, fetches each source XML, extracts the value via
\code{xmlstarlet} (a different XML toolchain than the package's own
\code{xml2}-based extractor), and compares to the value in the parquet.
Satisfies ADR 0002 acceptance criterion 5.
}
\details{
Independence is structural: the production extractor goes through
\code{xml2::read_xml} -> namespace-aware \code{xml_find_first} (binding the IRS
efile namespace to a prefix), while this spot-check goes through a
separate \code{xmlstarlet} process with its own explicit namespace binding.
A bug in one path is unlikely to mirror in the other.
}
