% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_nodc.R
\name{compare_dictionary_to_nodc}
\alias{compare_dictionary_to_nodc}
\title{Compare the NCCS Layer 2 dictionary against the NODC concordance}
\usage{
compare_dictionary_to_nodc(dict, nodc, config = load_config())
}
\arguments{
\item{dict}{Layer 2 dictionary (from \code{load_dictionary()}).}

\item{nodc}{NODC concordance data.frame (from
\code{load_nodc_concordance()}).}

\item{config}{Loaded config list.}
}
\value{
Invisibly, a list with \code{summary} (named counts + coverage)
and \code{results} (per-field data.frame).
}
\description{
The Phase 0.5 end-state (ADR 0017 section 3) demotes NODC from a
vendored build input to a \emph{comparison artifact}: at each NCCS
release an automated, informational diff reports where the curated
Layer 2 dictionary agrees with, diverges from, or is uncovered by
NODC's \code{concordance.csv}. It never blocks a build - it is a review
signal and a prioritization signal for growing Layer 2.
}
\details{
For each NCCS field carrying a \code{nodc_variable_name}, the comparison
checks the link both ways:
\itemize{
\item \code{xpath_agrees}        every XPath the NCCS field claims is among
NODC's known variants for that variable.
\item \code{xpath_diverges}      the NCCS field claims an XPath NODC does
not list (NCCS diverged, or NODC is stale).
\item \code{nodc_variable_absent}the referenced NODC variable is gone (a
broken NCCS->NODC link).
\item \code{no_nodc_link}        the NCCS field declares no NODC variable.
}
}
