% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xsd_inventory.R
\name{build_xsd_inventory}
\alias{build_xsd_inventory}
\title{Build the Layer 1 mechanical XSD inventory for one (tax_year, version)}
\usage{
build_xsd_inventory(
  tax_year,
  version,
  config = load_config(),
  xsd_dir = NULL,
  roots = NULL
)
}
\arguments{
\item{tax_year}{Integer or character tax year.}

\item{version}{XSD version string. If the configured
\code{xsd$version_aliases} redirects this (year, version) to another
version's XSDs (e.g. 2024v5.1 -> 5.0), the alias is honored.}

\item{config}{Loaded config list.}

\item{xsd_dir}{Optional explicit XSD directory (overrides the
cache-path + alias resolution).}

\item{roots}{Optional list of \code{list(name=, xpath=)} form roots to
enumerate from. Defaults to 990 + 990PF + ReturnHeader.}
}
\value{
A data.frame, one row per reachable element, with columns:
\code{tax_year}, \code{version}, \code{xpath}, \code{parent_path}, \code{element},
\code{xsd_type}, \code{is_leaf}, \code{truncated}, \code{min_occurs}, \code{max_occurs},
\code{annotation}. Deduplicated on \code{xpath}.
}
\description{
Enumerates every element reachable from the configured form roots in
an IRS XSD bundle, emitting one row per element with its full XPath,
parent path, declared XSD type, cardinality, and any
\code{xsd:documentation}. This is the "Layer 1" of the two-layer
concordance (ADR 0017 section 3): mechanical, never hand-edited,
regenerated on every IRS release. It is the inverse traversal of
\code{verify_xpath()}: that resolves a \emph{known} path top-down; this
enumerates \emph{all} paths by full depth-first descent.
}
\details{
Reuses \code{load_xsd_schema()} (the same flattened element/complexType
index the verifier builds). The inventory is keyed by position
(XPath), so a shared complexType reused under several parents yields
one row per occurrence - that is why the inventory is large
(~10K rows per form-version), not a bug.
}
