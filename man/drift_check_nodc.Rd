% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drift_check_nodc.R
\name{drift_check_nodc}
\alias{drift_check_nodc}
\title{Detect upstream drift in the NODC concordance file}
\usage{
drift_check_nodc(
  pinned_sha = NULL,
  config = load_config(),
  open_issue = FALSE,
  repo = NULL
)
}
\arguments{
\item{pinned_sha}{SHA currently vendored. Defaults to
\code{config$vendored$nodc_concordance_sha}.}

\item{config}{Loaded config list.}

\item{open_issue}{If \code{TRUE} and drift is detected, shell out to
\verb{gh issue create}. Requires \code{gh} authenticated in the current
environment.}

\item{repo}{\code{owner/name} for the issue target. Defaults to the
current git remote.}
}
\value{
Invisibly, a list with \code{drift} (logical), \code{pinned_sha},
\code{head_sha}, and (if drift) a \code{summary} list with \code{rows_added},
\code{rows_removed}, \code{rows_changed}, and \code{sample} (a small data.frame
of changed rows).
}
\description{
Compares the pinned NODC SHA against the current HEAD of the
upstream repo's default branch. On drift, downloads both versions,
summarizes the row-level diff, and (optionally) opens a GitHub
issue against this repo via the \code{gh} CLI.
}
\details{
See ADR 0001 section 8. Intended to be cron-scheduled monthly per
nccs-contracts ADR 0004 once the workflow lands.
}
