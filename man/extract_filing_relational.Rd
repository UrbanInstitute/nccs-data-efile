% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_relational.R
\name{extract_filing_relational}
\alias{extract_filing_relational}
\title{Extract one filing into its relational table rows.}
\usage{
extract_filing_relational(row, plan, max_bytes = 5e+07)
}
\arguments{
\item{row}{A single index row (list/1-row df) with \code{filing_receipt_id}, \code{ein},
\code{tax_year}, \code{form_type}, and \code{s3_key} or \code{local_path}.}

\item{plan}{A plan from \code{build_relational_plan()}.}

\item{max_bytes}{Unzipped-XML size cap; see \code{extract_filing}.}
}
\value{
A named list (by table) of one-row data.frames (keys + raw-string
value columns + \verb{_extract_error}), for the tables this filing populates.
}
\description{
Parses the DOM \strong{once} and, for each table the filing's \code{form_type} maps to
(always \code{returnheader}, plus the matching form body), pulls every scalar leaf
in the plan as raw text (typing is deferred to assembly). Mirrors
\code{extract_filing}'s robustness: fetch/parse/oversize failures yield a keyed
skeleton row with \verb{_extract_error} set, never an abort.
}
