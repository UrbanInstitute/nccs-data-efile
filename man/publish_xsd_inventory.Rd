% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xsd_inventory.R
\name{publish_xsd_inventory}
\alias{publish_xsd_inventory}
\title{Build, write, and (optionally) publish the Layer 1 XSD inventory.}
\usage{
publish_xsd_inventory(
  config = load_config(),
  out_dir = NULL,
  build_date = NULL,
  upload = TRUE,
  roots = NULL
)
}
\arguments{
\item{config}{Loaded config list.}

\item{out_dir}{Local directory to write into (default: a vintage dir
under \verb{out/layer1/}).}

\item{build_date}{\code{YYYY-MM-DD} string for the S3 vintage dir
(default: today, UTC).}

\item{upload}{If TRUE, sync the local dir to S3.}

\item{roots}{Optional form-root list.}
}
\value{
Invisibly, a list with \code{parquet_path}, \code{manifest_path},
\code{s3_prefix}, \code{rows}.
}
\description{
Produces a single consolidated parquet plus a provenance manifest
under \verb{concordance/layer1/\{build_date\}/} on S3 (with a \verb{latest/}
mirror), per ADR 0017 section 3 (Layer 1 is the mechanical layer of
the NCCS-owned concordance).
}
