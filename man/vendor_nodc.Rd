% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vendor_nodc.R
\name{vendor_nodc}
\alias{vendor_nodc}
\title{Vendor the NODC concordance file at a pinned SHA}
\usage{
vendor_nodc(
  sha = NULL,
  s3_prefix = NULL,
  config = load_config(),
  aws_profile = NULL,
  dry_run = FALSE
)
}
\arguments{
\item{sha}{Commit SHA to pin. Defaults to
\code{config$vendored$nodc_concordance_sha}.}

\item{s3_prefix}{Destination S3 prefix. Defaults to
\code{config$vendored$nodc_concordance_s3_prefix}.}

\item{config}{Loaded config list (see \code{\link[=load_config]{load_config()}}).}

\item{aws_profile}{AWS CLI profile name used for the \verb{aws s3} calls.}

\item{dry_run}{If \code{TRUE}, log actions without uploading.}
}
\value{
Invisibly, a list with \code{sha}, \code{s3_uri}, \code{bytes},
\code{sha256}, and \code{uploaded} (logical: \code{FALSE} if skipped because the
key already existed).
}
\description{
Downloads the NODC \code{irs-efile-master-concordance-file} repo's
\code{concordance.csv} at the configured commit SHA and mirrors it to
\verb{s3://nccsdata/processed/efile/concordance/\{sha\}_\{YYYY-MM-DD\}.csv}.
Idempotent: if the target S3 key already exists, the upload is
skipped (the GitHub raw file at a given SHA is immutable, so this
is safe).
}
\details{
See ADR 0001 section 8.
}
