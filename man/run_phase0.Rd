% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_phase0.R
\name{run_phase0}
\alias{run_phase0}
\title{Run a Phase 0 vintage build end-to-end}
\usage{
run_phase0(
  config = load_config(),
  dict = load_dictionary(),
  vintage = NULL,
  xsd_version,
  out_dir = NULL,
  skip_xsd_verification = FALSE,
  index = NULL
)
}
\arguments{
\item{config}{Loaded config list.}

\item{dict}{Layer-2 dictionary data.frame.}

\item{vintage}{Vintage string (\code{v2026.06}). Auto-derived if \code{NULL}.}

\item{xsd_version}{XSD version label to resolve XPath claims for.
Phase 0 uses one version per build.}

\item{out_dir}{Output directory. Defaults to
\code{file.path(config$output$local_dir, vintage)}.}

\item{skip_xsd_verification}{If \code{TRUE}, bypass the XSD check
(useful for local development before XSDs are cached).}

\item{index}{Optional pre-built index data.frame to skip the
fetch step (useful for testing).}
}
\value{
Invisibly, a summary list with vintage, output directory,
parquet paths, and the structured verification results.
}
\description{
Drives the full Phase 0 pipeline:
}
\details{
\enumerate{
\item Fetch the upstream index (GT data lake or IRS direct).
\item (Optional) Run XSD verification against the pinned XPath
claims; abort on mismatch unless \code{skip_xsd_verification}.
\item Extract every in-scope filing in parallel via \code{furrr}.
\item Verify the extracted value distribution against config
thresholds.
\item For each configured Phase 0 output, write a parquet plus its
dictionary and quality JSON.
\item Emit a per-vintage \verb{_manifest.json} covering all outputs.
}

S3 publication is deliberately not in this function - the driver
script handles \verb{aws s3 sync} after a successful build, so a failed
verification never leaves a half-published vintage.

See ADR 0001 section 4 (execution model) and section 10 (manifest).
}
