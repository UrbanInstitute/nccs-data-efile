% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_irs_direct.R
\name{fetch_irs_direct}
\alias{fetch_irs_direct}
\title{Fetch IRS-direct e-file ZIPs as a fallback to the GT data lake}
\usage{
fetch_irs_direct(
  tax_year,
  month,
  config = load_config(),
  local_cache_dir = NULL,
  url_pattern = NULL
)
}
\arguments{
\item{tax_year}{Integer or character tax year.}

\item{month}{Integer 1..12 - the calendar month of the IRS bundle.}

\item{config}{Loaded config list.}

\item{local_cache_dir}{Where to unpack ZIPs. Defaults to
\verb{~/.cache/nccs-data-efile/irs-direct/\{tax_year\}/\{month\}/}.}

\item{url_pattern}{Optional override; otherwise read from
\code{config$upstream$irs_direct$url_pattern}. Substitutions
\code{{tax_year}} and \code{{month}} (zero-padded) are honored.}
}
\value{
A data.frame with one row per XML in the unpacked bundle,
carrying \code{filing_receipt_id}, \code{ein}, \code{tax_period}, \code{tax_year},
\code{form_type}, \code{object_id}, \code{local_path}. \code{s3_key} is \code{NA} (these
filings did not come from S3).
}
\description{
Downloads the per-(tax_year, month) e-file ZIP bundle that the IRS
publishes on its Form 990 Series Downloads page, unpacks it under a
local cache directory, and returns an index data.frame matching the
shape of \code{fetch_gt_indices()} so the downstream \code{extract_filing()}
surface is unaware which upstream was used.
}
\details{
See ADR 0001 section 9. Activation is via \code{upstream.primary: irs_direct} in config. The exact ZIP URL pattern is configurable
because the IRS page layout has shifted historically.
}
