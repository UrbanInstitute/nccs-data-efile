% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify_dictionary_inventory.R
\name{verify_dictionary_against_inventory}
\alias{verify_dictionary_against_inventory}
\title{Verify the Layer 2 dictionary against the Layer 1 XSD inventory}
\usage{
verify_dictionary_against_inventory(
  dict,
  inventory,
  config = load_config(),
  strict = TRUE
)
}
\arguments{
\item{dict}{Layer 2 dictionary (from \code{load_dictionary()}).}

\item{inventory}{Layer 1 inventory data.frame (from
\code{load_layer1_inventory()} / \code{build_all_xsd_inventories()}).}

\item{config}{Loaded config list.}

\item{strict}{If TRUE, error on any HARD breach.}
}
\value{
Invisibly, a list with \code{passed}, \code{results} (per-claim
data.frame), and status counts.
}
\description{
Supply-side concordance gate (Phase 0.5): asserts that every
\code{xpath_claims} tuple in the curated dictionary points at an element
that actually exists in the mechanical Layer 1 inventory, and that
the element is a leaf (carries a value). This is the mechanical
successor to \code{verify_claim_coverage()} - that checks \emph{demand}
coverage (every in-scope filing's (year, version) resolves some
claim); this checks the claims themselves against ground truth, so a
typo'd or renamed XPath is an unrepresentable build state rather than
a silent stream of NAs.
}
\details{
Version strings are normalized before joining: the dictionary uses
dotted versions (matching the extractor's \code{parse_return_version})
while the inventory carries the IRS folder convention (hyphenated for
2022/2023). Comparing them raw is exactly the \code{5-0}-vs-\code{5.0} mismatch
that nulled TY2022/2023 in v2026.05 (ADR 0002 Outcome).

Claim statuses:
\itemize{
\item \code{ok}                     resolves to a leaf element of a
data_type-consistent XSD type.
\item \code{missing_xpath}          cell exists but the XPath is absent (HARD).
\item \code{not_leaf}               XPath exists but is a container (HARD).
\item \code{type_mismatch}          a numeric (\code{double}/\code{int}) field resolves
to a non-numeric XSD type (HARD). This is
the type-class check that replaced the
retired per-field \code{phase0_claims}
expected_type; non-numeric data_types are
recorded but not enforced yet.
\item \code{unverifiable_no_cell}   no inventory cell for that (year,
version) - a gated schema with no public
XSD. WARN, not a failure.
}

Coverage is bounded by the inventory's root scope (currently
990 + 990PF + ReturnHeader): a dictionary claim on a form/schedule
the inventory does not yet enumerate would read as \code{missing_xpath}.
Widen \code{default_inventory_roots()} before adding such claims.
}
