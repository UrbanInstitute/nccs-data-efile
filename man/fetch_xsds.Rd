% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_xsds.R
\name{fetch_xsds}
\alias{fetch_xsds}
\title{Fetch and cache IRS XSD bundles for a tax year + version}
\usage{
fetch_xsds(tax_year, version, config = load_config(), force = FALSE)
}
\arguments{
\item{tax_year}{Integer or character tax year.}

\item{version}{XSD version string.}

\item{config}{Loaded config list.}

\item{force}{If \code{TRUE}, re-download and re-upload regardless of
cache state.}
}
\value{
Invisibly, a list with \code{tax_year}, \code{version},
\code{local_dir}, \code{s3_prefix}, \code{downloaded} (logical), and
\code{mirrored} (logical).
}
\description{
Downloads the IRS XSD ZIP for \verb{(tax_year, version)}, unpacks it
under the local cache directory, and mirrors the contents to
\verb{s3://nccsdata/processed/efile/schemas/\{tax_year\}/\{version\}/}.
}
\details{
Idempotent: if the local cache directory already contains XSD
files, the download and unpack are skipped. If the S3 prefix
already contains objects, the mirror is skipped (cheap \verb{aws s3 ls}
check, not per-file ETag verification - if you need stronger
guarantees, pass \code{force = TRUE}).

See ADR 0001 section 5. The URL pattern lives in
\code{config$xsd$url_pattern} and is an open item until verified
against the IRS download pages.
}
